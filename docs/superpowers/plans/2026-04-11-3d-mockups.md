# 3D Mockups Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an interactive 3D product mockup page with Three.js WebGL viewer, logo texture mapping, and Gemini AI photorealistic enhancement for mugs and t-shirts.

**Architecture:** New Blazor page (`/3d-mockups`) with a full-width Three.js WebGL canvas, bottom toolbar controls, gallery picker for logo selection, and side-by-side AI comparison panel. Three.js loaded via CDN, IIFE module pattern for JS interop. New `Mockup3DService` handles AI enhancement via existing `IImageGenerationService`.

**Tech Stack:** Blazor Server, Three.js (CDN), GLTFLoader, OrbitControls, Gemini API (existing), SixLabors.ImageSharp

---

### Task 1: Register Feature in FeatureAccess and Program.cs

**Files:**
- Modify: `ArtForgeAI/Models/FeatureAccess.cs`
- Modify: `ArtForgeAI/Program.cs`

- [ ] **Step 1: Add feature constant and pricing to FeatureAccess.cs**

In `ArtForgeAI/Models/FeatureAccess.cs`, add the constant after `MockupStudio`:

```csharp
public const string Mockup3D = "Mockup3D";
```

Add to `PlanFeatures` — Free gets mug only (gated in page), Starter+ gets both:

```csharp
["Free"] = [QuickStyle, MockupStudio, Mockup3D],
["Starter"] = [QuickStyle, Home, StyleTransfer, StyleRemix, Gallery, MockupStudio, Mockup3D],
["Pro"] = [QuickStyle, Home, StyleTransfer, StyleRemix, Gallery, MosaicPoster, PassportPhoto, ImageViewer, Settings, NegativeScan, AutoEnhance, PhotoCollages, Merger, EmbroideryArt, BackgroundRemoval, SignatureDayDesign, FaceAging, EmotionTransfer, VideoCreator, MockupStudio, Mockup3D],
["Enterprise"] = [QuickStyle, Home, StyleTransfer, StyleRemix, Gallery, MosaicPoster, PassportPhoto, ImageViewer, Settings, PhotoExpand, GangSheet, ShapeCutSheet, NegativeScan, AutoEnhance, PhotoCollages, Merger, EmbroideryArt, BackgroundRemoval, SignatureDayDesign, FaceAging, EmotionTransfer, VideoCreator, MockupStudio, Mockup3D]
```

Add to `GenerationCosts`:

```csharp
[Mockup3D] = 2
```

Add to `AllFeatures` array — append `Mockup3D` at the end.

- [ ] **Step 2: Add NavMenu entry**

In `ArtForgeAI/Components/Layout/NavMenu.razor`, add after the MockupStudio entry in `AllFeatureNavItems`:

```csharp
new("Mockup3D", "3d-mockups", "&#129482;", "3D Mockups"),
```

- [ ] **Step 3: Register Mockup3DService in Program.cs**

In `ArtForgeAI/Program.cs`, add after the `MockupService` registration line:

```csharp
builder.Services.AddScoped<Mockup3DService>();
```

- [ ] **Step 4: Build and verify**

Run: `dotnet build`
Expected: Build succeeds (Mockup3DService doesn't exist yet, so this will fail — create a stub in next task first, or skip build until Task 2).

- [ ] **Step 5: Commit**

```bash
git add ArtForgeAI/Models/FeatureAccess.cs ArtForgeAI/Components/Layout/NavMenu.razor ArtForgeAI/Program.cs
git commit -m "feat: register Mockup3D feature in access control, nav, and DI"
```

---

### Task 2: Create Mockup3DService

**Files:**
- Create: `ArtForgeAI/Services/Mockup3DService.cs`

- [ ] **Step 1: Create the service file**

Create `ArtForgeAI/Services/Mockup3DService.cs`:

```csharp
using ArtForgeAI.Models;

namespace ArtForgeAI.Services;

public class Mockup3DService
{
    private readonly IImageGenerationService _imageGen;
    private readonly IImageStorageService _imageStorage;
    private readonly ILogger<Mockup3DService> _logger;

    public Mockup3DService(
        IImageGenerationService imageGen,
        IImageStorageService imageStorage,
        ILogger<Mockup3DService> logger)
    {
        _imageGen = imageGen;
        _imageStorage = imageStorage;
        _logger = logger;
    }

    /// <summary>
    /// Takes a Three.js canvas snapshot (base64 PNG) and sends it to Gemini
    /// for photorealistic AI enhancement.
    /// </summary>
    public async Task<GenerationResult> GenerateAi3DMockup(
        string snapshotBase64,
        string productName,
        string productColor,
        string userId)
    {
        // Decode base64 snapshot and save to storage
        var snapshotBytes = Convert.FromBase64String(snapshotBase64);
        var snapshotFileName = $"3d_snapshot_{Guid.NewGuid():N}.png";
        var snapshotPath = await _imageStorage.SaveImageFromBytesAsync(
            BinaryData.FromBytes(snapshotBytes), snapshotFileName);

        try
        {
            var prompt = $"Transform this 3D product render into a photorealistic product photograph. " +
                         $"The product is a {productColor} {productName}. " +
                         $"Keep the exact logo/design placement and appearance from the image. " +
                         $"Add realistic textures, reflections, ambient occlusion, and studio lighting. " +
                         $"Clean white background, professional product photography style. " +
                         $"High quality, sharp details.";

            var request = new GenerationRequest
            {
                Prompt = prompt,
                ReferenceImagePaths = [snapshotPath],
                Provider = ImageProvider.Gemini,
                EnhancePrompt = false,
                Width = 1536,
                Height = 1024,
                UserId = userId
            };

            return await _imageGen.GenerateImageAsync(request);
        }
        finally
        {
            // Clean up snapshot temp file
            try { await _imageStorage.DeleteImageAsync(snapshotPath); }
            catch { /* ignore cleanup failure */ }
        }
    }
}
```

- [ ] **Step 2: Build and verify**

Run: `dotnet build`
Expected: Build succeeds with 0 errors.

- [ ] **Step 3: Commit**

```bash
git add ArtForgeAI/Services/Mockup3DService.cs
git commit -m "feat: add Mockup3DService for 3D AI enhancement"
```

---

### Task 3: Download 3D Models

**Files:**
- Create: `ArtForgeAI/wwwroot/3d-models/mug.glb`
- Create: `ArtForgeAI/wwwroot/3d-models/tshirt.glb`

- [ ] **Step 1: Create the 3d-models directory**

```bash
mkdir -p ArtForgeAI/wwwroot/3d-models
```

- [ ] **Step 2: Download free mug model**

Download a free ceramic mug .glb model. Use a simple, clean model suitable for logo placement. Source from Sketchfab or similar with CC license.

```bash
# Example — use a free mug model from a CDN or Sketchfab download
# The model should be a standard coffee mug with a flat front surface
# If Sketchfab requires login, manually download and place the file
curl -L -o ArtForgeAI/wwwroot/3d-models/mug.glb "https://raw.githubusercontent.com/ArtForgeAI/3d-assets/main/mug.glb"
```

If no direct download is available, create a placeholder GLB using Three.js geometry exported to GLB. The engineer can replace it with a better model later. As a fallback, generate a simple mug programmatically in the JS code (Task 5 handles this).

- [ ] **Step 3: Download free t-shirt model**

Download a free t-shirt .glb model with a flat front surface for logo placement.

```bash
curl -L -o ArtForgeAI/wwwroot/3d-models/tshirt.glb "https://raw.githubusercontent.com/ArtForgeAI/3d-assets/main/tshirt.glb"
```

Same fallback applies — if no model is available, Task 5 will create procedural geometry.

- [ ] **Step 4: Verify files exist and are valid**

```bash
ls -la ArtForgeAI/wwwroot/3d-models/
```

Both `.glb` files should be present and non-empty (typical size: 100KB-2MB each).

- [ ] **Step 5: Commit**

```bash
git add ArtForgeAI/wwwroot/3d-models/
git commit -m "feat: add 3D model assets for mug and t-shirt"
```

**Note:** If `.glb` files are not available from free sources, the JS code in Task 5 includes a `createFallbackModel()` function that generates procedural geometry (cylinder+handle for mug, flat plane for t-shirt). The page will work either way.

---

### Task 4: Create CSS for the 3D Mockups Page

**Files:**
- Create: `ArtForgeAI/wwwroot/css/mockup3d.css`

- [ ] **Step 1: Create the CSS file**

Create `ArtForgeAI/wwwroot/css/mockup3d.css`:

```css
/* ─────────────────────────────────────────
   3D Mockups Page
───────────────────────────────────────── */

/* Full-width immersive layout */
.m3d-layout {
    display: flex;
    flex-direction: column;
    height: calc(100vh - 120px);
    gap: 0;
}

/* WebGL viewport — fills available space */
.m3d-viewport {
    flex: 1;
    position: relative;
    background: #1a1a2e;
    border: 2px solid var(--border-color);
    border-radius: var(--radius-lg) var(--radius-lg) 0 0;
    overflow: hidden;
    min-height: 300px;
}

.m3d-viewport canvas {
    width: 100% !important;
    height: 100% !important;
    display: block;
}

/* Loading spinner overlay */
.m3d-loading {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 0.75rem;
    background: rgba(26, 26, 46, 0.85);
    z-index: 10;
    color: var(--text-secondary);
    font-size: 0.85rem;
}

/* Upload logo button — centered in viewport when no logo */
.m3d-upload-prompt {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 5;
    text-align: center;
}

.m3d-upload-btn {
    background: linear-gradient(135deg, var(--primary), var(--primary-hover));
    color: #fff;
    border: none;
    border-radius: var(--radius-md);
    padding: 0.6rem 1.2rem;
    font-size: 0.85rem;
    cursor: pointer;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
}

.m3d-upload-btn:hover {
    transform: scale(1.05);
    box-shadow: 0 4px 15px rgba(147, 51, 234, 0.4);
}

/* WebGL not supported fallback */
.m3d-no-webgl {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 0.5rem;
    color: var(--text-muted);
    font-size: 0.9rem;
}

/* ── Bottom Toolbar ── */
.m3d-toolbar {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.6rem 1rem;
    background: var(--bg-secondary);
    border: 2px solid var(--border-color);
    border-top: none;
    border-radius: 0 0 var(--radius-lg) var(--radius-lg);
    flex-wrap: wrap;
}

/* Product tabs */
.m3d-product-tabs {
    display: flex;
    gap: 0.25rem;
}

.m3d-product-tab {
    padding: 0.3rem 0.7rem;
    font-size: 0.78rem;
    border-radius: var(--radius-sm);
    border: 1px solid var(--border-color);
    background: transparent;
    color: var(--text-secondary);
    cursor: pointer;
    transition: all 0.15s ease;
}

.m3d-product-tab.active {
    background: var(--primary);
    color: #fff;
    border-color: var(--primary);
}

.m3d-product-tab.locked {
    opacity: 0.4;
    cursor: not-allowed;
}

/* Color swatches */
.m3d-colors {
    display: flex;
    gap: 0.3rem;
    align-items: center;
}

.m3d-color-swatch {
    width: 22px;
    height: 22px;
    border-radius: 50%;
    border: 2px solid transparent;
    cursor: pointer;
    transition: border-color 0.15s ease;
}

.m3d-color-swatch.active {
    border-color: var(--primary);
}

/* Slider controls */
.m3d-slider-group {
    display: flex;
    align-items: center;
    gap: 0.3rem;
}

.m3d-slider-label {
    font-size: 0.7rem;
    color: var(--text-muted);
    min-width: 28px;
}

.m3d-slider {
    width: 80px;
    accent-color: var(--primary);
    height: 4px;
}

/* Toolbar separator */
.m3d-sep {
    width: 1px;
    height: 24px;
    background: var(--border-color);
}

/* Action buttons */
.m3d-actions {
    display: flex;
    gap: 0.4rem;
    margin-left: auto;
}

/* ── Logo picker modal ── */
.m3d-modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.6);
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: center;
}

.m3d-modal {
    background: var(--bg-secondary);
    border: 2px solid var(--border-color);
    border-radius: var(--radius-lg);
    width: 90%;
    max-width: 600px;
    max-height: 80vh;
    display: flex;
    flex-direction: column;
}

.m3d-modal-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.75rem 1rem;
    border-bottom: 1px solid var(--border-color);
}

.m3d-modal-header h3 {
    margin: 0;
    font-size: 0.95rem;
    color: var(--text-primary);
}

.m3d-modal-close {
    background: none;
    border: none;
    color: var(--text-muted);
    font-size: 1.2rem;
    cursor: pointer;
}

.m3d-modal-tabs {
    display: flex;
    border-bottom: 1px solid var(--border-color);
}

.m3d-modal-tab {
    flex: 1;
    padding: 0.5rem;
    text-align: center;
    font-size: 0.8rem;
    background: none;
    border: none;
    color: var(--text-secondary);
    cursor: pointer;
    border-bottom: 2px solid transparent;
}

.m3d-modal-tab.active {
    color: var(--primary);
    border-bottom-color: var(--primary);
}

.m3d-modal-body {
    padding: 1rem;
    overflow-y: auto;
    flex: 1;
}

/* Gallery grid inside modal */
.m3d-gallery-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0.5rem;
}

.m3d-gallery-item {
    aspect-ratio: 1;
    border-radius: var(--radius-sm);
    overflow: hidden;
    cursor: pointer;
    border: 2px solid transparent;
    transition: border-color 0.15s ease;
}

.m3d-gallery-item:hover {
    border-color: var(--primary);
}

.m3d-gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

/* ── Side-by-side AI comparison ── */
.m3d-comparison {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-top: 1rem;
    background: var(--bg-secondary);
    border: 2px solid var(--border-color);
    border-radius: var(--radius-lg);
    padding: 1rem;
    position: relative;
}

.m3d-comparison-close {
    position: absolute;
    top: 0.5rem;
    right: 0.5rem;
    background: var(--bg-tertiary);
    border: 1px solid var(--border-color);
    color: var(--text-secondary);
    border-radius: 50%;
    width: 28px;
    height: 28px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;
}

.m3d-comparison-panel {
    text-align: center;
}

.m3d-comparison-label {
    font-size: 0.75rem;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 0.5rem;
}

.m3d-comparison-panel img {
    max-width: 100%;
    max-height: 350px;
    border-radius: var(--radius-sm);
    background: var(--bg-tertiary);
}

.m3d-comparison-panel button {
    margin-top: 0.5rem;
}

/* ── Error message ── */
.m3d-error {
    padding: 0.6rem 1rem;
    margin-top: 0.5rem;
    border-radius: var(--radius-sm);
    background: rgba(239, 68, 68, 0.15);
    color: var(--danger, #ef4444);
    font-size: 0.85rem;
    border-left: 3px solid var(--danger, #ef4444);
}

/* ── Responsive ── */
@media (max-width: 1024px) {
    .m3d-toolbar {
        gap: 0.6rem;
    }
    .m3d-slider {
        width: 60px;
    }
}

@media (max-width: 768px) {
    .m3d-layout {
        height: auto;
    }
    .m3d-viewport {
        min-height: 50vh;
    }
    .m3d-toolbar {
        overflow-x: auto;
        flex-wrap: nowrap;
        scrollbar-width: none;
    }
    .m3d-toolbar::-webkit-scrollbar {
        display: none;
    }
    .m3d-comparison {
        grid-template-columns: 1fr;
    }
    .m3d-gallery-grid {
        grid-template-columns: repeat(3, 1fr);
    }
}
```

- [ ] **Step 2: Commit**

```bash
git add ArtForgeAI/wwwroot/css/mockup3d.css
git commit -m "feat: add CSS for 3D Mockups page"
```

---

### Task 5: Create Three.js Scene Module (mockup3d.js)

**Files:**
- Create: `ArtForgeAI/wwwroot/js/mockup3d.js`

- [ ] **Step 1: Create the JavaScript module**

Create `ArtForgeAI/wwwroot/js/mockup3d.js`:

```javascript
window.mockup3d = (function () {
    let scene, camera, renderer, controls;
    let currentModel = null;
    let logoMesh = null;
    let logoTexture = null;
    let animationId = null;
    let dotnetRef = null;

    // Logo transform state
    let logoSize = 0.5;
    let logoVPos = 0.0;
    let logoHPos = 0.0;

    function initScene(canvasId, dotnetObjRef) {
        dotnetRef = dotnetObjRef;
        const container = document.getElementById(canvasId);
        if (!container) return false;

        // Check WebGL support
        const testCanvas = document.createElement('canvas');
        const gl = testCanvas.getContext('webgl2') || testCanvas.getContext('webgl');
        if (!gl) return false;

        // Scene
        scene = new THREE.Scene();
        scene.background = new THREE.Color(0x1a1a2e);

        // Camera
        camera = new THREE.PerspectiveCamera(45, container.clientWidth / container.clientHeight, 0.1, 100);
        camera.position.set(0, 0.5, 2.5);

        // Renderer
        renderer = new THREE.WebGLRenderer({ antialias: true, preserveDrawingBuffer: true });
        renderer.setSize(container.clientWidth, container.clientHeight);
        renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
        renderer.outputColorSpace = THREE.SRGBColorSpace;
        renderer.toneMapping = THREE.ACESFilmicToneMapping;
        renderer.toneMappingExposure = 1.2;
        container.appendChild(renderer.domElement);

        // Controls
        controls = new THREE.OrbitControls(camera, renderer.domElement);
        controls.enableDamping = true;
        controls.dampingFactor = 0.08;
        controls.minPolarAngle = Math.PI * 0.2;
        controls.maxPolarAngle = Math.PI * 0.8;
        controls.minDistance = 1.0;
        controls.maxDistance = 5.0;

        // Lighting — 3-point studio
        const keyLight = new THREE.DirectionalLight(0xfff5e6, 1.5);
        keyLight.position.set(3, 4, 2);
        scene.add(keyLight);

        const fillLight = new THREE.DirectionalLight(0xe6f0ff, 0.8);
        fillLight.position.set(-3, 3, 1);
        scene.add(fillLight);

        const rimLight = new THREE.DirectionalLight(0xffffff, 0.4);
        rimLight.position.set(0, 2, -3);
        scene.add(rimLight);

        const ambientLight = new THREE.AmbientLight(0xffffff, 0.3);
        scene.add(ambientLight);

        // Resize handler
        window.addEventListener('resize', onResize);

        // Start render loop
        animate();
        return true;
    }

    function onResize() {
        if (!renderer) return;
        const container = renderer.domElement.parentElement;
        if (!container) return;
        const w = container.clientWidth;
        const h = container.clientHeight;
        camera.aspect = w / h;
        camera.updateProjectionMatrix();
        renderer.setSize(w, h);
    }

    function animate() {
        animationId = requestAnimationFrame(animate);
        if (controls) controls.update();
        if (renderer && scene && camera) renderer.render(scene, camera);
    }

    function loadModel(modelUrl) {
        // Clean up previous model
        if (currentModel) {
            scene.remove(currentModel);
            currentModel.traverse(function (child) {
                if (child.geometry) child.geometry.dispose();
                if (child.material) {
                    if (Array.isArray(child.material)) {
                        child.material.forEach(m => m.dispose());
                    } else {
                        child.material.dispose();
                    }
                }
            });
            currentModel = null;
        }
        removeLogo();

        const loader = new THREE.GLTFLoader();
        loader.load(
            modelUrl,
            function (gltf) {
                currentModel = gltf.scene;

                // Center and scale model
                const box = new THREE.Box3().setFromObject(currentModel);
                const center = box.getCenter(new THREE.Vector3());
                const size = box.getSize(new THREE.Vector3());
                const maxDim = Math.max(size.x, size.y, size.z);
                const scale = 1.5 / maxDim;
                currentModel.scale.setScalar(scale);
                currentModel.position.sub(center.multiplyScalar(scale));

                scene.add(currentModel);

                // Re-apply logo if one exists
                if (logoTexture) {
                    applyLogoToModel();
                }

                if (dotnetRef) {
                    dotnetRef.invokeMethodAsync('OnModelLoaded');
                }
            },
            undefined,
            function (error) {
                console.error('Model load error:', error);
                // Try fallback procedural model
                loadFallbackModel(modelUrl);
            }
        );
    }

    function loadFallbackModel(modelUrl) {
        const isMug = modelUrl.includes('mug');

        if (isMug) {
            // Procedural mug: cylinder body + torus handle
            const group = new THREE.Group();

            const bodyGeom = new THREE.CylinderGeometry(0.4, 0.35, 0.7, 32);
            const bodyMat = new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.3, metalness: 0.05 });
            const body = new THREE.Mesh(bodyGeom, bodyMat);
            body.name = 'body';
            group.add(body);

            // Handle
            const handleGeom = new THREE.TorusGeometry(0.2, 0.04, 12, 24, Math.PI);
            const handleMat = new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.3, metalness: 0.05 });
            const handle = new THREE.Mesh(handleGeom, handleMat);
            handle.position.set(0.45, 0, 0);
            handle.rotation.z = Math.PI / 2;
            group.add(handle);

            // Inside rim
            const rimGeom = new THREE.TorusGeometry(0.38, 0.02, 8, 32);
            const rimMat = new THREE.MeshStandardMaterial({ color: 0xdddddd, roughness: 0.2 });
            const rim = new THREE.Mesh(rimGeom, rimMat);
            rim.position.y = 0.35;
            rim.rotation.x = Math.PI / 2;
            group.add(rim);

            currentModel = group;
            scene.add(currentModel);
        } else {
            // Procedural t-shirt: flat plane
            const group = new THREE.Group();

            const bodyGeom = new THREE.PlaneGeometry(1.2, 1.4, 1, 1);
            const bodyMat = new THREE.MeshStandardMaterial({
                color: 0xffffff, roughness: 0.8, metalness: 0.0, side: THREE.DoubleSide
            });
            const body = new THREE.Mesh(bodyGeom, bodyMat);
            body.name = 'body';
            group.add(body);

            // Sleeves
            const sleeveGeom = new THREE.PlaneGeometry(0.5, 0.4);
            const sleeveMat = new THREE.MeshStandardMaterial({
                color: 0xffffff, roughness: 0.8, side: THREE.DoubleSide
            });

            const leftSleeve = new THREE.Mesh(sleeveGeom, sleeveMat);
            leftSleeve.position.set(-0.8, 0.35, 0);
            leftSleeve.rotation.z = -0.4;
            group.add(leftSleeve);

            const rightSleeve = new THREE.Mesh(sleeveGeom.clone(), sleeveMat.clone());
            rightSleeve.position.set(0.8, 0.35, 0);
            rightSleeve.rotation.z = 0.4;
            group.add(rightSleeve);

            currentModel = group;
            scene.add(currentModel);
        }

        if (logoTexture) applyLogoToModel();
        if (dotnetRef) dotnetRef.invokeMethodAsync('OnModelLoaded');
    }

    function setColor(hexColor) {
        if (!currentModel) return;
        const color = new THREE.Color(hexColor);
        currentModel.traverse(function (child) {
            if (child.isMesh && child.name !== 'logo') {
                child.material.color.copy(color);
            }
        });
    }

    function setLogo(dataUrl) {
        if (!dataUrl) {
            removeLogo();
            return;
        }

        const img = new Image();
        img.crossOrigin = 'anonymous';
        img.onload = function () {
            if (logoTexture) logoTexture.dispose();

            const canvas = document.createElement('canvas');
            canvas.width = img.width;
            canvas.height = img.height;
            const ctx = canvas.getContext('2d');
            ctx.drawImage(img, 0, 0);

            logoTexture = new THREE.CanvasTexture(canvas);
            logoTexture.colorSpace = THREE.SRGBColorSpace;

            applyLogoToModel();
        };
        img.src = dataUrl;
    }

    function applyLogoToModel() {
        if (!currentModel || !logoTexture) return;

        removeLogo();

        // Create a plane for the logo decal
        const aspect = logoTexture.image.width / logoTexture.image.height;
        const planeW = logoSize * aspect;
        const planeH = logoSize;
        const geometry = new THREE.PlaneGeometry(planeW, planeH);
        const material = new THREE.MeshBasicMaterial({
            map: logoTexture,
            transparent: true,
            depthWrite: false,
            polygonOffset: true,
            polygonOffsetFactor: -1
        });

        logoMesh = new THREE.Mesh(geometry, material);
        logoMesh.name = 'logo';

        positionLogo();
        scene.add(logoMesh);
    }

    function positionLogo() {
        if (!logoMesh || !currentModel) return;

        // Get model bounding box
        const box = new THREE.Box3().setFromObject(currentModel);
        const center = box.getCenter(new THREE.Vector3());
        const size = box.getSize(new THREE.Vector3());

        // Position logo on front face
        logoMesh.position.set(
            center.x + (logoHPos * size.x * 0.4),
            center.y + (logoVPos * size.y * 0.4),
            box.max.z + 0.01
        );

        // Update geometry size
        const aspect = logoTexture.image.width / logoTexture.image.height;
        logoMesh.geometry.dispose();
        logoMesh.geometry = new THREE.PlaneGeometry(logoSize * aspect, logoSize);
    }

    function updateLogoTransform(size, vPos, hPos) {
        logoSize = size;
        logoVPos = vPos;
        logoHPos = hPos;
        if (logoMesh) positionLogo();
    }

    function removeLogo() {
        if (logoMesh) {
            scene.remove(logoMesh);
            if (logoMesh.geometry) logoMesh.geometry.dispose();
            if (logoMesh.material) logoMesh.material.dispose();
            logoMesh = null;
        }
    }

    function captureSnapshot(width, height) {
        if (!renderer || !scene || !camera) return null;

        // Save current size
        const origW = renderer.domElement.width;
        const origH = renderer.domElement.height;

        // Resize for high-res capture
        renderer.setSize(width, height);
        camera.aspect = width / height;
        camera.updateProjectionMatrix();
        renderer.render(scene, camera);

        const dataUrl = renderer.domElement.toDataURL('image/png');

        // Restore original size
        renderer.setSize(origW, origH);
        camera.aspect = origW / origH;
        camera.updateProjectionMatrix();

        // Return only the base64 data (strip prefix)
        return dataUrl.split(',')[1];
    }

    function dispose() {
        if (animationId) cancelAnimationFrame(animationId);
        window.removeEventListener('resize', onResize);

        if (logoMesh) removeLogo();
        if (logoTexture) { logoTexture.dispose(); logoTexture = null; }
        if (currentModel) {
            scene.remove(currentModel);
            currentModel.traverse(function (child) {
                if (child.geometry) child.geometry.dispose();
                if (child.material) {
                    if (Array.isArray(child.material)) child.material.forEach(m => m.dispose());
                    else child.material.dispose();
                }
            });
            currentModel = null;
        }
        if (renderer) {
            renderer.dispose();
            renderer.domElement.remove();
            renderer = null;
        }
        if (controls) { controls.dispose(); controls = null; }
        scene = null;
        camera = null;
        dotnetRef = null;
    }

    return {
        initScene,
        loadModel,
        setColor,
        setLogo,
        updateLogoTransform,
        captureSnapshot,
        dispose
    };
})();
```

- [ ] **Step 2: Commit**

```bash
git add ArtForgeAI/wwwroot/js/mockup3d.js
git commit -m "feat: add Three.js scene module for 3D Mockups"
```

---

### Task 6: Create the Mockup3D Blazor Page

**Files:**
- Create: `ArtForgeAI/Components/Pages/Mockup3D.razor`

- [ ] **Step 1: Create the page file**

Create `ArtForgeAI/Components/Pages/Mockup3D.razor`:

```razor
@page "/3d-mockups"
@rendermode InteractiveServer
@attribute [Authorize]
@inject IJSRuntime JS
@inject IWebHostEnvironment Env
@inject ILogger<Mockup3D> Logger
@inject IImageStorageService ImageStorage
@inject IGenerationHistoryService HistoryService
@inject ICoinService CoinService
@inject ISubscriptionService SubscriptionService
@inject AuthenticationStateProvider AuthState
@inject Mockup3DService Mockup3DSvc
@using ArtForgeAI.Services
@using ArtForgeAI.Models
@implements IDisposable

<PageTitle>ArtForge AI - 3D Mockups</PageTitle>

<link href="css/mockup3d.css" rel="stylesheet" />

<script src="https://unpkg.com/three@0.162.0/build/three.min.js"></script>
<script src="https://unpkg.com/three@0.162.0/examples/js/controls/OrbitControls.js"></script>
<script src="https://unpkg.com/three@0.162.0/examples/js/loaders/GLTFLoader.js"></script>
<script src="js/mockup3d.js"></script>

<div class="page-header page-header-lg">
    <h2>3D Mockups</h2>
    <p>Interactive 3D product mockups — rotate, zoom, and place your logo</p>
</div>

@if (!string.IsNullOrEmpty(_errorMessage))
{
    <div class="m3d-error">@_errorMessage</div>
}

<div class="m3d-layout">
    @* ── 3D Viewport ── *@
    <div class="m3d-viewport">
        <div id="m3d-canvas-container" style="width:100%;height:100%;"></div>

        @if (_isModelLoading)
        {
            <div class="m3d-loading">
                <div class="loading-spinner"></div>
                <span>@_loadingStatus</span>
            </div>
        }

        @if (!_isModelLoading && !_webglSupported)
        {
            <div class="m3d-no-webgl">
                <span style="font-size:2rem;">&#9888;</span>
                <span>WebGL is not supported in your browser.</span>
                <span>Please use a modern browser like Chrome, Firefox, or Edge.</span>
            </div>
        }

        @if (_webglSupported && string.IsNullOrEmpty(_logoDataUrl) && !_isModelLoading)
        {
            <div class="m3d-upload-prompt">
                <button class="m3d-upload-btn" @onclick="() => _showLogoModal = true">
                    &#128083; Upload or Select Logo
                </button>
            </div>
        }

        @if (_isProcessing)
        {
            <div class="m3d-loading">
                <div class="loading-spinner"></div>
                <span>@_processingStatus</span>
            </div>
        }
    </div>

    @* ── Bottom Toolbar ── *@
    <div class="m3d-toolbar">
        @* Product tabs *@
        <div class="m3d-product-tabs">
            @foreach (var product in _products)
            {
                var p = product;
                var locked = !_availableProducts.Contains(p.Slug);
                <button class="m3d-product-tab @(_activeProduct.Slug == p.Slug ? "active" : "") @(locked ? "locked" : "")"
                        @onclick="() => SwitchProduct(p)"
                        disabled="@locked"
                        title="@(locked ? "Upgrade plan to unlock" : p.Name)">
                    @p.Name
                </button>
            }
        </div>

        <div class="m3d-sep"></div>

        @* Color swatches *@
        <div class="m3d-colors">
            @foreach (var color in _activeProduct.Colors)
            {
                var c = color;
                <div class="m3d-color-swatch @(_selectedColor == c ? "active" : "")"
                     style="background-color:@c;"
                     @onclick="() => SetColor(c)"
                     title="@c"></div>
            }
        </div>

        <div class="m3d-sep"></div>

        @* Logo controls (only when logo is loaded) *@
        @if (!string.IsNullOrEmpty(_logoDataUrl))
        {
            <div class="m3d-slider-group">
                <span class="m3d-slider-label">Size</span>
                <input type="range" class="m3d-slider" min="0.1" max="1.5" step="0.05"
                       @bind="_logoSize" @bind:event="oninput" @oninput="OnLogoTransformChanged" />
            </div>

            <div class="m3d-slider-group">
                <span class="m3d-slider-label">&#8597;</span>
                <input type="range" class="m3d-slider" min="-1.0" max="1.0" step="0.05"
                       @bind="_logoVPos" @bind:event="oninput" @oninput="OnLogoTransformChanged" />
            </div>

            <div class="m3d-slider-group">
                <span class="m3d-slider-label">&#8596;</span>
                <input type="range" class="m3d-slider" min="-1.0" max="1.0" step="0.05"
                       @bind="_logoHPos" @bind:event="oninput" @oninput="OnLogoTransformChanged" />
            </div>

            <button class="btn-secondary" style="font-size:0.75rem;padding:0.2rem 0.5rem;"
                    @onclick="() => _showLogoModal = true">
                &#128260; Change
            </button>

            <div class="m3d-sep"></div>
        }

        @* Actions *@
        <div class="m3d-actions">
            @if (string.IsNullOrEmpty(_logoDataUrl))
            {
                <button class="btn-primary-gradient" style="font-size:0.8rem;padding:0.35rem 0.8rem;"
                        @onclick="() => _showLogoModal = true">
                    &#128083; Add Logo
                </button>
            }
            <button class="btn-primary-gradient" style="font-size:0.8rem;padding:0.35rem 0.8rem;"
                    @onclick="Download3DRender" disabled="@(_isProcessing || !_webglSupported)">
                &#128424; Download &middot; 2
            </button>
            <button class="btn-secondary" style="font-size:0.8rem;padding:0.35rem 0.8rem;"
                    @onclick="AiEnhance" disabled="@(_isProcessing || !_webglSupported)">
                &#10024; AI Enhance &middot; 5
            </button>
        </div>
    </div>
</div>

@* ── Side-by-side AI comparison ── *@
@if (_showComparison && !string.IsNullOrEmpty(_snapshotDataUrl) && !string.IsNullOrEmpty(_aiResultDataUrl))
{
    <div class="m3d-comparison">
        <button class="m3d-comparison-close" @onclick="CloseComparison">&#10005;</button>

        <div class="m3d-comparison-panel">
            <div class="m3d-comparison-label">3D Render</div>
            <img src="@_snapshotDataUrl" alt="3D Render" />
            <div>
                <button class="btn-primary-gradient" style="font-size:0.8rem;padding:0.3rem 0.8rem;margin-top:0.5rem;"
                        @onclick="DownloadSnapshot">
                    &#128424; Download 3D Render &middot; 2
                </button>
            </div>
        </div>

        <div class="m3d-comparison-panel">
            <div class="m3d-comparison-label">AI Enhanced</div>
            <img src="@_aiResultDataUrl" alt="AI Enhanced" />
            <div>
                <button class="btn-primary-gradient" style="font-size:0.8rem;padding:0.3rem 0.8rem;margin-top:0.5rem;"
                        @onclick="DownloadAiResult">
                    &#128424; Download AI Mockup
                </button>
            </div>
        </div>
    </div>
}

@* ── Logo Picker Modal ── *@
@if (_showLogoModal)
{
    <div class="m3d-modal-overlay" @onclick="() => _showLogoModal = false">
        <div class="m3d-modal" @onclick:stopPropagation="true">
            <div class="m3d-modal-header">
                <h3>Select Logo</h3>
                <button class="m3d-modal-close" @onclick="() => _showLogoModal = false">&#10005;</button>
            </div>

            <div class="m3d-modal-tabs">
                <button class="m3d-modal-tab @(_modalTab == "upload" ? "active" : "")"
                        @onclick='() => _modalTab = "upload"'>Upload</button>
                <button class="m3d-modal-tab @(_modalTab == "gallery" ? "active" : "")"
                        @onclick="OpenGalleryTab">Gallery</button>
            </div>

            <div class="m3d-modal-body">
                @if (_modalTab == "upload")
                {
                    <label class="upload-area upload-area-compact" style="min-height:120px;">
                        <span class="upload-icon">&#128083;</span>
                        <span>Upload a logo or image</span>
                        <span style="font-size:0.7rem;color:var(--text-muted);">PNG, JPG, WebP — transparent background recommended</span>
                        <InputFile OnChange="HandleFileUpload" accept=".jpg,.jpeg,.png,.webp" style="display:none" />
                    </label>
                }
                else
                {
                    @if (_galleryImages == null)
                    {
                        <div style="text-align:center;padding:2rem;">
                            <div class="loading-spinner"></div>
                        </div>
                    }
                    else if (_galleryImages.Count == 0)
                    {
                        <p style="text-align:center;color:var(--text-muted);padding:2rem;">No images in gallery yet.</p>
                    }
                    else
                    {
                        <div class="m3d-gallery-grid">
                            @foreach (var img in _galleryImages)
                            {
                                var imgPath = img;
                                <div class="m3d-gallery-item" @onclick="() => SelectGalleryImage(imgPath)">
                                    <img src="@imgPath" alt="Gallery image" loading="lazy" />
                                </div>
                            }
                        </div>
                    }
                }
            </div>
        </div>
    </div>
}

@code {
    // ── Product definitions ──
    private record Product3D(string Name, string Slug, string ModelFile, string[] Colors);

    private static readonly Product3D[] _products =
    [
        new("Mug", "mug", "3d-models/mug.glb", ["#FFFFFF", "#222222", "#C0C0C0"]),
        new("T-Shirt", "tshirt", "3d-models/tshirt.glb", ["#FFFFFF", "#222222", "#C0C0C0", "#1B3A5C", "#C0392B"]),
    ];

    // ── State ──
    private Product3D _activeProduct = _products[0];
    private string _selectedColor = "#FFFFFF";
    private string? _logoDataUrl;
    private float _logoSize = 0.5f;
    private float _logoVPos = 0f;
    private float _logoHPos = 0f;

    private bool _isModelLoading = true;
    private string _loadingStatus = "Loading 3D model...";
    private bool _isProcessing;
    private string _processingStatus = "";
    private string? _errorMessage;
    private bool _webglSupported = true;

    private bool _showLogoModal;
    private string _modalTab = "upload";
    private List<string>? _galleryImages;

    private bool _showComparison;
    private string? _snapshotDataUrl;
    private string? _aiResultDataUrl;
    private string? _aiResultPath;

    private int _userId;
    private bool _isSuperAdmin;
    private string _userPlan = "Free";
    private HashSet<string> _availableProducts = new() { "mug" };

    private DotNetObjectReference<Mockup3D>? _dotnetRef;

    // ── Lifecycle ──

    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthState.GetAuthenticationStateAsync();
        var user = authState.User;
        if (int.TryParse(user.FindFirst("UserId")?.Value, out var uid))
            _userId = uid;
        _isSuperAdmin = user.IsInRole("SuperAdmin");

        _userPlan = await SubscriptionService.GetCurrentPlanNameAsync(_userId);

        // Determine available products based on plan
        if (_userPlan != "Free" || _isSuperAdmin)
        {
            _availableProducts = new HashSet<string> { "mug", "tshirt" };
        }
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (!firstRender) return;

        _dotnetRef = DotNetObjectReference.Create(this);
        var supported = await JS.InvokeAsync<bool>("mockup3d.initScene", "m3d-canvas-container", _dotnetRef);

        if (!supported)
        {
            _webglSupported = false;
            _isModelLoading = false;
            StateHasChanged();
            return;
        }

        await JS.InvokeVoidAsync("mockup3d.loadModel", _activeProduct.ModelFile);
    }

    // ── JS Callbacks ──

    [JSInvokable]
    public void OnModelLoaded()
    {
        _isModelLoading = false;
        StateHasChanged();
    }

    [JSInvokable]
    public void OnModelError(string message)
    {
        _isModelLoading = false;
        _errorMessage = "Failed to load 3D model: " + message;
        StateHasChanged();
    }

    // ── Product & Color ──

    private async Task SwitchProduct(Product3D product)
    {
        if (!_availableProducts.Contains(product.Slug)) return;
        if (_activeProduct.Slug == product.Slug) return;

        _activeProduct = product;
        _selectedColor = product.Colors[0];
        _logoSize = 0.5f;
        _logoVPos = 0f;
        _logoHPos = 0f;
        _isModelLoading = true;
        _loadingStatus = $"Loading {product.Name}...";
        _showComparison = false;
        _snapshotDataUrl = null;
        _aiResultDataUrl = null;
        StateHasChanged();

        await JS.InvokeVoidAsync("mockup3d.loadModel", product.ModelFile);
    }

    private async Task SetColor(string color)
    {
        _selectedColor = color;
        await JS.InvokeVoidAsync("mockup3d.setColor", color);
    }

    // ── Logo ──

    private async Task HandleFileUpload(InputFileChangeEventArgs e)
    {
        _errorMessage = null;
        var file = e.File;
        if (file.Size > 10 * 1024 * 1024)
        {
            _errorMessage = "File too large. Max 10MB.";
            return;
        }

        using var stream = file.OpenReadStream(10 * 1024 * 1024);
        using var ms = new MemoryStream();
        await stream.CopyToAsync(ms);
        var bytes = ms.ToArray();
        var ext = Path.GetExtension(file.Name).TrimStart('.').ToLower();
        if (ext == "jpg") ext = "jpeg";
        _logoDataUrl = $"data:image/{ext};base64," + Convert.ToBase64String(bytes);

        _showLogoModal = false;
        StateHasChanged();

        await JS.InvokeVoidAsync("mockup3d.setLogo", _logoDataUrl);
    }

    private async Task SelectGalleryImage(string imagePath)
    {
        _logoDataUrl = imagePath;
        _showLogoModal = false;
        StateHasChanged();

        // Convert web path to data URL for Three.js
        var fullPath = Path.Combine(Env.WebRootPath, imagePath.Replace("/", Path.DirectorySeparatorChar.ToString()).TrimStart(Path.DirectorySeparatorChar));
        if (File.Exists(fullPath))
        {
            var bytes = await File.ReadAllBytesAsync(fullPath);
            var dataUrl = "data:image/png;base64," + Convert.ToBase64String(bytes);
            await JS.InvokeVoidAsync("mockup3d.setLogo", dataUrl);
        }
    }

    private async Task OpenGalleryTab()
    {
        _modalTab = "gallery";
        if (_galleryImages == null)
        {
            try
            {
                var history = await HistoryService.GetHistoryAsync(_userId.ToString(), 1, 40);
                _galleryImages = history
                    .Where(h => !string.IsNullOrEmpty(h.LocalImagePath))
                    .Select(h => h.LocalImagePath!)
                    .ToList();
            }
            catch
            {
                _galleryImages = new List<string>();
            }
        }
    }

    private async Task OnLogoTransformChanged()
    {
        await JS.InvokeVoidAsync("mockup3d.updateLogoTransform", _logoSize, _logoVPos, _logoHPos);
    }

    // ── Download ──

    private async Task Download3DRender()
    {
        if (_isProcessing) return;
        _isProcessing = true;
        _processingStatus = "Capturing 3D render...";
        _errorMessage = null;
        StateHasChanged();

        try
        {
            if (!await TryDebitCoins(2, "3D Mockups download"))
            {
                _isProcessing = false;
                StateHasChanged();
                return;
            }

            var base64 = await JS.InvokeAsync<string>("mockup3d.captureSnapshot", 1920, 1080);
            if (string.IsNullOrEmpty(base64))
            {
                _errorMessage = "Failed to capture render.";
                return;
            }

            var fileName = $"3d_{_activeProduct.Slug}_{DateTime.Now:yyyyMMdd_HHmmss}.png";
            await JS.InvokeVoidAsync("downloadFileFromBytes", fileName, "image/png", base64);
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "3D download failed");
            _errorMessage = "Download failed: " + ex.Message;
        }
        finally
        {
            _isProcessing = false;
            _processingStatus = "";
            StateHasChanged();
        }
    }

    // ── AI Enhancement ──

    private async Task AiEnhance()
    {
        if (_isProcessing) return;
        _isProcessing = true;
        _processingStatus = $"Generating AI-enhanced {_activeProduct.Name}...";
        _errorMessage = null;
        _showComparison = false;
        StateHasChanged();

        try
        {
            if (!await TryDebitCoins(5, "3D Mockups AI enhance"))
            {
                _isProcessing = false;
                StateHasChanged();
                return;
            }

            // Capture snapshot
            var base64 = await JS.InvokeAsync<string>("mockup3d.captureSnapshot", 1536, 1024);
            if (string.IsNullOrEmpty(base64))
            {
                _errorMessage = "Failed to capture render for AI enhancement.";
                return;
            }

            _snapshotDataUrl = "data:image/png;base64," + base64;

            // Send to AI
            var colorName = _selectedColor switch
            {
                "#FFFFFF" => "white",
                "#222222" => "black",
                "#C0C0C0" => "grey",
                "#1B3A5C" => "navy blue",
                "#C0392B" => "red",
                _ => "colored"
            };

            var result = await Mockup3DSvc.GenerateAi3DMockup(
                base64, _activeProduct.Name, colorName, _userId.ToString());

            if (result.Success && !string.IsNullOrEmpty(result.LocalImagePath))
            {
                var fullPath = Path.Combine(Env.WebRootPath,
                    result.LocalImagePath.Replace("/", Path.DirectorySeparatorChar.ToString()).TrimStart(Path.DirectorySeparatorChar));
                var resultBytes = await File.ReadAllBytesAsync(fullPath);
                _aiResultDataUrl = "data:image/png;base64," + Convert.ToBase64String(resultBytes);
                _aiResultPath = fullPath;
                _showComparison = true;
            }
            else
            {
                _errorMessage = "AI enhancement failed: " + (result.ErrorMessage ?? "Unknown error");
            }
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "AI enhance failed");
            _errorMessage = "AI enhancement failed: " + ex.Message;
        }
        finally
        {
            _isProcessing = false;
            _processingStatus = "";
            StateHasChanged();
        }
    }

    private async Task DownloadSnapshot()
    {
        if (string.IsNullOrEmpty(_snapshotDataUrl)) return;
        if (!await TryDebitCoins(2, "3D Mockups render download"))
        {
            StateHasChanged();
            return;
        }
        var base64 = _snapshotDataUrl[((_snapshotDataUrl.IndexOf(",", StringComparison.Ordinal) + 1))..];
        var fileName = $"3d_{_activeProduct.Slug}_render_{DateTime.Now:yyyyMMdd_HHmmss}.png";
        await JS.InvokeVoidAsync("downloadFileFromBytes", fileName, "image/png", base64);
    }

    private async Task DownloadAiResult()
    {
        if (string.IsNullOrEmpty(_aiResultDataUrl)) return;
        var base64 = _aiResultDataUrl[((_aiResultDataUrl.IndexOf(",", StringComparison.Ordinal) + 1))..];
        var fileName = $"3d_{_activeProduct.Slug}_ai_{DateTime.Now:yyyyMMdd_HHmmss}.png";
        await JS.InvokeVoidAsync("downloadFileFromBytes", fileName, "image/png", base64);
    }

    private void CloseComparison()
    {
        _showComparison = false;
        _snapshotDataUrl = null;
        _aiResultDataUrl = null;
    }

    // ── Coins ──

    private async Task<bool> TryDebitCoins(int cost, string description)
    {
        if (_isSuperAdmin) return true;
        if (!await CoinService.DebitCoinsAsync(_userId, cost, CoinTransactionType.GenerationSpend, description))
        {
            _errorMessage = "Insufficient coins. Please top up to continue.";
            StateHasChanged();
            return false;
        }
        return true;
    }

    // ── Cleanup ──

    public void Dispose()
    {
        _ = JS.InvokeVoidAsync("mockup3d.dispose");
        _dotnetRef?.Dispose();
    }
}
```

- [ ] **Step 2: Build and verify**

Run: `dotnet build`
Expected: Build succeeds with 0 errors.

- [ ] **Step 3: Commit**

```bash
git add ArtForgeAI/Components/Pages/Mockup3D.razor
git commit -m "feat: add 3D Mockups page with Three.js viewer and AI enhancement"
```

---

### Task 7: Integration Test — End-to-End Verification

**Files:** No new files — manual testing.

- [ ] **Step 1: Start the application**

```bash
cd ArtForgeAI && dotnet run
```

- [ ] **Step 2: Verify navigation**

Open `http://localhost:7027` in browser. Check that "3D Mockups" appears in the sidebar after "Mockup Studio". Click it — should navigate to `/3d-mockups`.

- [ ] **Step 3: Verify 3D viewport**

The page should show a dark viewport with a 3D mug (or procedural fallback) that can be rotated by mouse drag and zoomed with scroll wheel.

- [ ] **Step 4: Test product switching**

Click "T-Shirt" tab (if unlocked). The mug should be replaced with the t-shirt model. Click "Mug" to switch back.

- [ ] **Step 5: Test color changes**

Click different color swatches. The 3D model color should update instantly.

- [ ] **Step 6: Test logo upload**

Click "Upload or Select Logo" button. Upload a PNG. The logo should appear as a decal on the 3D model front face. Adjust size/position sliders — logo should move in real-time.

- [ ] **Step 7: Test gallery picker**

Click "Change" button, switch to Gallery tab. Previously generated images should appear. Click one to select it as the logo.

- [ ] **Step 8: Test download**

Click "Download · 2". Should capture a high-res PNG of the current 3D view and trigger a browser download (costs 2 coins).

- [ ] **Step 9: Test AI enhance**

Click "AI Enhance · 5". Should show a loading spinner, then display the side-by-side comparison panel below the viewport with the 3D render on left and AI-enhanced version on right. Both download buttons should work.

- [ ] **Step 10: Test responsive layout**

Resize the browser window to <768px width. The toolbar should become a scrollable horizontal strip. The comparison panel should stack vertically.

- [ ] **Step 11: Final commit**

If any fixes were needed during testing, commit them:

```bash
git add -A
git commit -m "fix: address issues found during 3D Mockups integration testing"
```
